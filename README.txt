Simple chest selection puzzle game. The rules are simple:
    -You're given three chests
    -One chest contains poison, the other two contain a key
    to the next round
    -make it to the end and you win, choose poison and the game
    is over

Compile and run using "make run" command in a terminal.

Remove executable using "make clean" command in a terminal.